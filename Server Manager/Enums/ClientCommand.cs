public enum ClientCommand
{
    GetServerManagerCon = 0, // Get ServerManager.con
    SendServerManagerCon = 1, // Send ServerManager.con <data>
    GetPunkBusterServerConfig = 2, // Get PB cfg
    SendPunkBusterServerConfig = 3, // Send PB cfg
    PbDump = 4, // Dump PB Settings to pbsv.cfg on server
    PbLoad = 5, // Load PB Settings from pbsv.cfg on server
    GetModsCon = 8, // Get Mods.con
    GetMapsCon = 9, // Get Maps.con
    GetServerMapListCon = 10, // Get ServerMapList.con
    SendServerMaplistCon = 11, // Send ServerMapList.con <data>
    GetPlayerListCon = 12, // Get player list
    GetClientListCon = 13, // Get connected manager clients
    GetPlayerMenuCon = 14, // Get PlayerMenu.con
    GetServerBanListCon = 15, // Get BanList.con
    SendServerBanListCon = 16, // Send BanList.con <data>
    GetUserAccessCon = 17, // Get UserAccess.con (truncated file to only users with same or less access)
    SendUserAccessCon = 18, // Send UserAccess.con <data>
    GetServerScheduleCon = 19, // Get ServerSchedule.con
    SendServerScheduleCon = 20, // Send ServerSchedule.con <data>
    GetStatus = 21, // Get server status
    GetAnnouncementsCon = 22, // Get Announcements.con
    SendAnnouncementsCon = 23, // Send Announcements.con <data>
    GetBannedWordsCon = 24, // Get BannedWords.con
    SendBannedWordsCon = 25, // Send BannedWords.con <data>
    StartBf1942Server = 26, // Command to start server (no response)
    StopBf1942Server = 27, // Command to stop server (no response)
    PauseGame = 28, // Command server to pause/unpause (responds with cmd 01/19/1a)
    GetHostPort = 29, // Return 14567,1
    ChangeMap = 30, // Set map to <str> '<map> <mode> <mod>'
    RunNextMap = 31, // Run next map
    SetNextMap = 32, // Set next map <str> '<map> <mode> <mod>'
    SetIdleMap = 33, // Set Idle map <str> '<map> <mode> <mod>'
    RestartMap = 34, // Restart map
    Kick = 35, // Kick player <str> '<num>,<reason>,'
    DisconnectClient = 36, // Disconnect client operator at console <client id>
    Ban = 37, // Ban player <str> '-1\xff<name>\xff<addr>\xff[unknown]\xff[unknown]\xff<expires>\xff<reason>\xff\x00'
    Unban = 38, // Unban player <str> '<name>,addr,[unknown],[unknown]'
    UnbanAll = 39, // Remove all bans
    BanName = 40, // Ban player with name
    SyncVerifyBanlist = 41, // Sync with the server banlist
    SyncToBanlist = 42, // Sync to the server banlist
    SyncFromBanlist = 43, // Sync from the server banlist
    Chat = 44, // Issue grey message (same as game.sayAll)
    Message = 45, // Issue yellow message
    ConsoleCommand = 46, // Issue console command
    SetPassword = 47, // Set new password <str>
    GetServerManagerLog = 48, // Get BFServerManager.log
    Screenshot = 49, // Take screenshot <str>, player num +1 (Returns cmd 1 from server)
    Keepalive = 50, // Keepalive from client
}
